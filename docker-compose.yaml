services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    volumes:
      - h2-data:/app/data
    restart: unless-stopped

volumes:
  h2-data:
